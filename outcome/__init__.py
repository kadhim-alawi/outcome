"""OUTCOME — an autonomous phone-work agent that owns a goal to resolution.

CALL-E is a tool inside this agent, not the agent itself. The engine plans,
calls, reads the result as evidence, checks it against the user's constraints,
and replans until the goal is reached or it runs out of budget.
"""

__version__ = "0.1.0"
