__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
*.sqlite3
*.db
runs/
.env

# Generated scenarios that carry a real phone number. This repository is
# public and the numbers in it are all fictional; a live run needs a real one,
# so it is written here and never committed.
scenarios/local/

# Only needed to run scripts/time_demo.mjs, which measures the demo pacing.
node_modules/
package.json
package-lock.json
